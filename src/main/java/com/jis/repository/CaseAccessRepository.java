package com.jis.repository;

import com.jis.entity.CaseAccess;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CaseAccessRepository extends JpaRepository<CaseAccess, Integer> {
}