# Cloud profile defaults; sensitive values come from environment variables.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Cookies should be secure in HTTPS production environments.
jwt.cookie-secure=true
jwt.cookie-same-site=None
